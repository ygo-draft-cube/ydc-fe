import { CardImageUrlPipe } from './card-image-url.pipe';

describe('CardImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CardImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
