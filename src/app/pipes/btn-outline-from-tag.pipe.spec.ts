import { BtnOutlineFromTagPipe } from './btn-outline-from-tag.pipe';

describe('BtnOutlineFromTagPipe', () => {
  it('create an instance', () => {
    const pipe = new BtnOutlineFromTagPipe();
    expect(pipe).toBeTruthy();
  });
});
