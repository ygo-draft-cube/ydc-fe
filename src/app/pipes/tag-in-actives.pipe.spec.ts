import { TagInActivesPipe } from './tag-in-actives.pipe';

describe('TagInActivesPipe', () => {
  it('create an instance', () => {
    const pipe = new TagInActivesPipe();
    expect(pipe).toBeTruthy();
  });
});
