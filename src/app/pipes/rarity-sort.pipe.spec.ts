import { RaritySortPipe } from './rarity-sort.pipe';

describe('RaritySortPipe', () => {
  it('create an instance', () => {
    const pipe = new RaritySortPipe();
    expect(pipe).toBeTruthy();
  });
});
