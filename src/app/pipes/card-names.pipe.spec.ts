import { CardNamesPipe } from './card-names.pipe';

describe('CardNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CardNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
