import { FilterRarityPipe } from './filter-rarity.pipe';

describe('FilterRarityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRarityPipe();
    expect(pipe).toBeTruthy();
  });
});
