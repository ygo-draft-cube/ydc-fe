import { RemoveDupsPipe } from './remove-dups.pipe';

describe('RemoveDupsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDupsPipe();
    expect(pipe).toBeTruthy();
  });
});
