import {CoverImageUrlPipe} from './cover-image-url.pipe';

describe('CoverImageUrlPipe', () => {
    it('create an instance', () => {
        const pipe = new CoverImageUrlPipe();
        expect(pipe).toBeTruthy();
    });
});
